#include<stdio.h>
int main()
{
int n;
unsigned long long factorial=1;
printf ("Enter a Number");
scanf("%d",&n);
for(int i=1;i<=n;i++)
{
factorial*=1;
}
printf ("The Factorial of %d is %llub",n,factorial);
return 0;
}
